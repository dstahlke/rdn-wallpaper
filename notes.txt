TODO:
    good defaults
    default downsample -> 3
    downsample/res don't initially show numbers
    maybe save a per-palette hue
    test fresh install
    website

To take screenshot:
    Exit the monitor
    adb jdwp
    adb forward tcp:7777 jdwp:<nnn>
    jdb -sourcepath src -attach localhost:7777
        stop in org.stahlke.rdnwallpaper.RdnWallpaper$MyEngine.drawFrame
    Open the monitor and take screenshot
    Exit jdb
